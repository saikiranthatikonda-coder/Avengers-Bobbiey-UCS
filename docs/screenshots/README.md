# Screenshots

Drop dashboard captures here to showcase the command center on the repo's front page.

Suggested set (the main README can embed these names):
- `dashboard.png` — full command center
- `threat-center.png` — Threat Intelligence row (matrix + SOC + timeline)
- `orb.png` — the reactor orb with the rotating Earth
- `ai-ops.png` — AI Operations row (insights + reasoning + vision)

**How to add:** with the dashboard running at `http://127.0.0.1:8765`, press `PrtSc` (Windows) or `Cmd+Shift+4` (macOS), save the PNGs here, then:
```bash
git add docs/screenshots && git commit -m "Add dashboard screenshots" && git push
```
Then re-add the image grid to the main README's Screenshots section.
